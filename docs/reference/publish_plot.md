# Create dynamic graphic from plot object when document rendered as HTML

Create dynamic graphic from plot object when document rendered as HTML

## Usage

``` r
publish_plot(plot)
```

## Arguments

- plot:

  ggplot object
