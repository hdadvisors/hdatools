% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes.R
\name{theme_vha}
\alias{theme_vha}
\title{Use a VHA-branded ggplot2 theme}
\usage{
theme_vha(
  base_size = 13,
  base_family = "Montserrat",
  flip_gridlines = FALSE,
  output_format = NULL,
  html_adjust = 4,
  pdf_adjust = 7,
  ...
)
}
\arguments{
\item{base_size}{The base size of text elements; defaults to 13}

\item{base_family}{The base font family; defaults to "Montserrat"}

\item{flip_gridlines}{Orientation of major gridlines; defaults to FALSE for y-axis}

\item{output_format}{Optional manual specification of output format}

\item{html_adjust}{Amount subtracted from base_size for HTML output; defaults to 4}

\item{pdf_adjust}{Amount subtracted from base_size for PDF output; defaults to 7}

\item{...}{Additional arguments passed to ggplot2::theme()}
}
\description{
Use a VHA-branded ggplot2 theme
}
\details{
When overriding strip text under ggplot2 >= 4.0, use
\code{ggtext::element_markdown()}, never a raw \code{ggplot2::element_text()}: the
branded strip element is a ggtext markdown element, and ggplot2 4.0 only
merges theme elements of the same class.

The theme alone carries VHA's brand identity into an otherwise
unbranded plot: bar/column fills, point colors, and line colors default
to \code{vha_colors["Dark Turq"]} (via \code{ggplot2::element_geom()}), a discrete
\code{aes(colour =)}/\code{aes(fill =)} mapping cycles through the full VHA
palette, and a continuous mapping uses the VHA sequential ramp
(see \code{\link[=scale_color_vha_c]{scale_color_vha_c()}}) — all with no \verb{scale_*()} call required.
An explicit \verb{scale_*()} (or a manual \code{aes()} value) always overrides
these theme-carried defaults.
}
