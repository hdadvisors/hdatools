# Decisions log

> Append-only. One row per settled decision. Before proposing a change that
> touches a decided area, check here — a settled decision is not re-litigated
> unless implementation surfaces a contradiction, in which case raise it with
> Jonathan explicitly and record the revision as a **new row** (don't edit the
> old one).
>
> "Q#" rows refer to the open questions in
> [archive/hdatools-design-review.md §3](archive/hdatools-design-review.md).
> OPEN rows get filled at their phase gate (see ROADMAP.md).

## Settled

| Date | Ref | Decision | Rationale | Binds |
|---|---|---|---|---|
| 2026-07-16 | process | **Git flow:** one long-lived branch per release phase; sessions commit to it; single PR to main at phase end, after CI green + release checklist. Tidyverse conventions throughout. | Keeps `main` always-installable for floating consumers; matches R-community best practice. | all phases |
| 2026-07-16 | process | **Open questions settle via phase-gate interviews** — short structured interview at each phase's first session, covering only that phase's questions; answers recorded here immediately. | Decide when the information is freshest; don't lock font/CVD calls months early. | all phases |
| 2026-07-16 | process | **Plan files: one per release phase**, written just-in-time, plus ROADMAP.md + this log. Completed plans move to `plans/archive/`. | Phase-level granularity shows the arc; JIT drafting lets interview answers shape later plans. | plans/ folder |
| 2026-07-16 | process | **Phase 0 exists**: CI + pha-update-2026 survey + consumer-pinning notes land before any refactor. | Refactor needs a safety net and full blast-radius knowledge. | Phase 0 |
| 2026-07-16 | review | **Compatibility contract: fhfh's surface is the only inviolable one** — `theme_hda()` signature + `...` passthrough + `flip_gridlines`, `add_zero_line("x"/"y")`, `scale_fill_hda()` named args. Everything else may change behind `lifecycle` soft-deprecations. | Per Jonathan during the design review; faar/others are archived or small. | Phases 1–3 |
| 2026-07-16 | review | **Design goal: brands are data, not code.** Adding a client brand = one registry entry (+ optional bundled fonts); palettes, scales, and a theme fall out of it. | PHA/VHA precedent; recurring client work. | architecture |
| 2026-07-16 | review | **Release sequencing:** Tier 1 → 0.3.0, Tier 2 → 0.4.0, fonts → 0.5.0; brand.yml/vdiffr/use_hdatools deferred to 0.6.0+; package split (§3.5) rejected. | Isolates the rendering-change release (fonts) so diffs are attributable. | phase table |
| 2026-07-17 | Q1 | **Palette registry shape:** internal `.brands` named list in `R/brands.R` — one entry per brand holding `palette` (named hex vector), `gradient` (stop vector), `fonts` (title/body), `na_color`, and theme parameters. Internal only in Phase 1; Phase 2 exports `*_colors` vectors from it. | Single lookup for everything a brand needs; Sessions 2–3 read from it without restructuring; consistent with "brands are data" goal; `[[name]]` access syntax carries through to Phase 2 public API unchanged. | Phase 1 `R/brands.R` shape; Phase 2 `*_colors` export; Q2 brand.yml is an *output* from the registry, not an input |
| 2026-07-17 | Q3 | **Deprecation policy:** fhfh surface inviolable (no change ever to `theme_hda()` signature + `...` passthrough + `flip_gridlines`, `add_zero_line`, `scale_fill_hda()` named args). Soft-deprecate `hda_pal_discrete()`, `hfv_pal_discrete()`, `pha_pal_discrete()`, `scale_color_gradient_hda()`, `scale_color_gradient_pha()`, and `scale_fill_gradient_pha()` in 0.3.0 via `lifecycle::deprecate_soft()` — bare (no `use_instead`). `lifecycle` enters `Imports` in Phase 1. Removals at 0.5.0+. | faar delivered/frozen; pha-update-2026 not started; vhtf and fed-workforce confirmed not using gradient scales; 0.4.0 follows immediately with designed replacements. | Phase 1 (lifecycle in Imports, deprecations land in Session 2); Phase 2 (replacement functions carry the `use_instead` target); Phase 3 (removals) |
| 2026-07-18 | Q2 | **Adopt `_brand.yml` as an output only** — generate one per brand from the `.brands` registry so Quarto document theming matches chart theming; the package never parses it back in at runtime. | Accepted recommended default. | Posture settled at Phase 2 gate; implementation stays Phase 4 backlog (3.2) per the 2026-07-16 release-sequencing decision |
| 2026-07-18 | Q4 | **Raise the ggplot2 dependency floor to ≥ 4.0** at the 0.4.0 release; no dual-pathing 3.5/4.0 in the theme builder. | Accepted recommended default. | Phase 2 item 2.3 (theme-carried palettes, `ink`/`paper`, `element_geom()` defaults); DESCRIPTION `Imports` bump |
| 2026-07-18 | Q6 | **Continuous ramps built programmatically** from each brand's anchor colors via `colorspace` HCL interpolation (fixed luminance sweep), then eyeballed and CVD-checked before shipping — not hand-designed per brand. | Accepted recommended default. | Phase 2 item 2.2 (sequential + diverging ramps, `scale_*_{brand}_c()` matrix) |
| 2026-07-18 | Q7 | **CVD audit is document-only this phase — no palette reordering.** Where a palette fails badly, add an opt-in `cvd_safe = TRUE` argument or a separately-named safe variant rather than changing default color order. | Accepted recommended default; reordering silently recolors every existing multi-series chart in downstream consumers on next render. | Phase 2 item 2.5 (CVD audit + `simulate_cvd` regression test) |
| 2026-07-18 | Q8 | **Add VHA as a fourth first-class brand** — `theme_vha()`, palettes, scales, all from one `.brands` registry entry; bundle Montserrat (OFL). Confirm VHA hex ownership before coding the registry entry. | Accepted recommended default; vhtf currently maintains a ~200-line local clone that becomes retirable after this ships. | Phase 2 item 2.4; unblocks vhtf's own follow-up to retire its local VHA clone (ROADMAP.md ledger) |
| 2026-07-18 | Q9 | **Keep house theme defaults** (`legend.position = "none"`, blank axis titles) — no change. | Accepted recommended default; ~150 downstream call sites depend on them, and legends are already reachable via `...` passthrough. | All themes (`.brand_theme()` builder); reaffirms the fhfh-inviolable-surface decision (2026-07-16) |
| 2026-07-18 | 2.2 | **Six ramp winners settled** (Ramp Lab review, exact params + verified hex in `plans/ramp-lab/REVIEW.md`): HDA seq Blue→cream (c64, p0.85); HDA div Blue vs Coral (c76, l26, p0.9) — *provisional, see HDA-differentiation row*; HFV seq Leaf→cream (generated defaults); HFV div Leaf vs Berry (c68, l26, p0.95); PHA seq Red→cream (c84, p0.95); PHA div Dark Blue vs Red (c72, l26, p0.9). **Scope: these six only** — one sequential + one diverging per brand; Berry accent ramp rejected for now (deutan dark-boundary collapse, ΔE 8.9). | Reviewed across all preview modes + 3 CVD sims; every pick's R snippet verified against real colorspace. Smallest API, no CVD-caveated ramps. | Phase 2 item 2.2 build (`scale_*_{brand}_c()` matrix); ramp params become registry data |
| 2026-07-18 | brand | **Leaf (#6fb547) and Cerulean (#7fc7e0) join the HFV brand palette** as official secondary colors (2 new entries in the `.brands` HFV registry + `hfv_colors`). | Both HFV ramp picks are built on Leaf; Jonathan supplied both hexes for the ramp lab and approved adoption at pick sign-off. Cerulean adopted as a palette color even though unused by ramps. | Phase 2 items 2.1 (`hfv_colors` export) + 2.2; `.brands` registry |
| 2026-07-18 | 2.2 | **Protan diverging caveat accepted + documented, not redesigned:** both cool-warm divergings lose sign distinction in the innermost pale class pair under protanopia (structural to the shared cream center). Ships with a documented usage rule — 7-class diverging maps always carry a legend/labels. | Alternative (neutral/cool-biased center) breaks the shared-cream system across all six ramps. | Ramp documentation (roxygen + pkgdown article); Phase 2 item 2.5 CVD-audit wording |
| 2026-07-19 | 2.5 | **HDA Green/Sea Green tritanopia collapse accepted as document-only** — audit found delta-E = 5.97 under full tritanopia (positions 2/6 in the palette); no palette reorder. Document recommends a secondary encoding if CVD robustness is critical. HFV Sky/Grass (~12 delta-E under all CVD types) also documented; no action. PHA Orange/Red flag cleared (≥ 22 delta-E across all CVD types). | Tritanopia affects ~0.1 % of population; Green/Sea Green co-occur only in ≥5-category plots. Per Q7 posture: document-only unless Jonathan explicitly signs off on a reorder. | `vignettes/articles/cvd-audit.Rmd`; `tests/testthat/test-cvd.R` regression guards |
| 2026-07-18 | 2.2 | **HDA diverging to be differentiated from PHA's before final adoption** — the two are visual siblings (navy vs warm brick). Follow-up ramp-lab pass: push HDA's cool arm teal-forward (or equivalent), then re-run the CVD checks. PHA's diverging is final as picked (its only structurally sound pair). | HDA and PHA charts can plausibly share a page; HDA is the lever because PHA has no alternative pair (Purple vs Green fails outright). | Blocks HDA `scale_*_hda_c()` diverging only; other five ramps unblocked |

## Open — settle at phase gates

| Ref | Question (short) | Recommended default (review §3) | Gate |
|---|---|---|---|
| Q1 | Palette single source of truth? | Plain named character vectors in an internal registry + exported vectors; not `data/`, not brand.yml-driven | Phase 1 |
| Q3 | Deprecate old names or break them? | fhfh surface inviolable; soft-deprecate `scale_*_gradient_*` and `*_pal_discrete()` now, remove 0.5+ | Phase 1 |
| Q2 | Adopt `_brand.yml`? | Yes as an *output* generated from the registry (Tier 3.2), never a runtime input | Phase 2 |
| Q4 | Raise ggplot2 floor to ≥ 4.0? | Yes, at the 0.4.0 release (theme-carried palettes need it) | Phase 2 |
| Q6 | Continuous ramp design method? | Programmatic from brand anchors via colorspace HCL, then eyeballed + CVD-checked | Phase 2 |
| Q7 | Reorder palettes for CVD safety? | Audit + document now; reorder only with explicit sign-off (it recolors existing charts) | Phase 2 |
| Q8 | Add VHA as first-class brand? | Yes; confirm hex ownership, bundle Montserrat (OFL) | Phase 2 |
| Q9 | Keep house theme defaults (no legend, blank axis titles)? | Keep — ~150 call sites depend on them | Phase 2 |
| Q5 | Migrate fonts to systemfonts/ragg? | Yes, in a dedicated release with a side-by-side render comparison in one consumer first | Phase 3 |
| Q10 | `add_reliability()` — anything more? | Verify against fhfh's real data in *fhfh's* session, then retire its ban there | n/a (fhfh repo) |
